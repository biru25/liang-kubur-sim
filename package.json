{
  "name": "liang-kubur-sim",
  "version": "1.0.0",
  "private": true,
  "description": "Simulasi liang kubur (pendidikan Islam) + voice Deepgram",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "pages:build": "node -e \"console.log('Cloudflare Pages: static public/ + functions/')\"",
    "pages:deploy": "env -u CLOUDFLARE_API_TOKEN -u CF_API_TOKEN wrangler pages deploy . --project-name=liang-kubur-sim --commit-dirty=true",
    "deploy:cf": "npm run pages:deploy"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "wrangler": "^4.44.0"
  }
}